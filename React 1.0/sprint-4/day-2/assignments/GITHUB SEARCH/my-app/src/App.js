import "./App.css";

// create a component that allows you to search for users on github
// use https://api.github.com/search/users api
// docs
// on fetching show the results in a list of Card components
// use axios to fetch data
// apply pagination, per page limit ( use useEffect to manage these as dependencies )

function App() {
  return (
    <div className="App">
      <header className="App-header"></header>
    </div>
  );
}

export default App;
