// import { useEffect, useState } from "react";

// const useTimeout = (prop) => {
//   const [showText, setShowText] = useState(false);

//   useEffect(() => {
//     let timer = setTimeout(() => {
//       setShowText(true);
//     }, prop);

//     return () => {
//       clearTimeout(timer);
//     };
//   }, [showText]);

//   return showText;
// };

// export default useTimeout;
