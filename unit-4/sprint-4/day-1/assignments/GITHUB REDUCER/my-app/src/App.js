import "./App.css";

// Create a component github, search for users on load of the component
// Create a reducer and manage fetching with loading, success and failure from there
// use the dispatchers, ensure you're keeping the loading and error indicators are inside the reducer state

function App() {
  return (
    <div className="App">
      <header className="App-header"></header>
    </div>
  );
}

export default App;
